// ESERCIZIO CONSEGNA:

/*
1. Crea un array di numeri chiamato "numbers" contenente almeno 5 numeri.
2. Usa il metodo .map() per creare un nuovo array "doubled" in cui ogni numero dell'array "numbers" è raddoppiato.
3. Usa il metodo .filter() per creare un array "even" che contenga solo i numeri pari dall'array "numbers".
4. Crea una funzione `processNumbers` che prenda due callback come argomenti: una per raddoppiare i numeri (usando .map()) e una per filtrare i numeri pari (usando .filter()).
5. Usa `setInterval` per chiamare la funzione `processNumbers` ogni 4 secondi, passando come argomento l'array "numbers" e le due callback. Il risultato di ogni operazione deve essere stampato a console.
6. Dopo 20 secondi, usa `setTimeout` per fermare l'esecuzione dell'intervallo.
7. Utilizza lo spread operator e lo spread parameter nelle funzioni per lavorare con gli array in modo più elegante.
8. Utilizza il destructuring per accedere agli elementi dell'array e passare i parametri nelle funzioni quando necessario.
*/

